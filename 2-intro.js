//console.log(__dirname)
//console.log(__filename)
//require  -function to use modules (commonJS)
//module  -info about current module (file)
//console.log(module)
//process  -info about env where the program is being executed
//console.log(process)



/*
    npm - global command, comes with node
        npm --v //to check the version

    local dependency - install the package and use it only in this particular project
        npm i <packageName>

    global dependency - to install it and use it in any project
        npm install -g <packageName>

    package.json manifest file (stores important infos about our project/package)
        three ways to create package.json
            -manual approach (create package.json in the root, create properties etc)
            -npm init (step by step, press enter to skip)
            -npm init -y (everything default)
    */